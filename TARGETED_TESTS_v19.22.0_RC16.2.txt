49 targeted tests passed
4 subtests passed
0 failures

Validated contracts:
- Rapporter imports and calls render_shared_manual_job_progress.
- Rapporter no longer calls the RC16.1 module-level progress fragment.
- Shared component uses the same dynamic fragment(run_every="5s") as Autonomi Oversikt.
- Shared component uses get_active_status().
- Rapporter disables terminal full-app rerun.
- Existing Autonomi Oversikt progress contract remains present.
