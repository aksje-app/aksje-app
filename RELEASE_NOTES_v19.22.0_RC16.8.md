# Release notes v19.22.0 RC16.8

Shared Draft and Live ZIP Progress Hotfix.

Reports now starts drafts through the exact same engine entry point as Autonomy Overview. The duplicate report-center start path and its explicit full-app rerun were removed.

Complete report/replay ZIP creation now publishes granular file-based progress, current stage and current file, while the UI polls only the isolated export fragment.
