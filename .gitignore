# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/

# Virtual environments / local env
.venv/
venv/
env/
.env
.env.*
!.env.example

# Streamlit / local config
.streamlit/secrets.toml

# Runtime storage: keep only folder placeholders in Git.
# App/runtime data should go through StorageService/Postgres, with local files only as fallback.
data/**
!data/
!data/.gitkeep
!data/forecasts/
!data/forecasts/.gitkeep
!data/services/
!data/services/.gitkeep
forecast_learning_stats.json
forecast_log.jsonl
forecast_alerts.jsonl
*_latest.json

# App runtime state / local fallback stores
paper_portfolio.json
app_users.json
remember_tokens.json
app_settings.json
alert_state.json
trading_rules.json
strategy_test_logs.json
strategy_profiles.json

# Cache / logs / temp
cache/
logs/
runtime/
*.log
*.tmp
.DS_Store

# Local databases / exports
*.sqlite
*.sqlite3
*.db
*.dump

# Local Render/env helper files
.render/
