Continue from v18.5.33 Sidebar Cleanup + Header Busy Hardfix. This version keeps the consolidated header status area, moves panel/status/trading controls to the top workspace, tightens the sidebar user/admin UI, removes the large sidebar user-table box, makes the warning text above trading buttons compact, and fixes top-right version/Klar overlap by using a clear inline busy slot with spinner while running. Header should show Professional Trading Workspace v18.5.33. Runtime data is excluded; data/, data/forecasts/ and data/services/ contain only .gitkeep.
