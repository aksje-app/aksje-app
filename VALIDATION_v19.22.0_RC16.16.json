{
  "version": "v19.22.0-rc16.16",
  "status": "LOCAL_PASS_LIVE_REQUIRED",
  "python_compile": "PASS",
  "targeted_unittest": {"passed": 20, "failed": 0},
  "verified": [
    "form submission removed from archive start",
    "non-periodic fragment owns a normal start button",
    "on_click callback calls start_export exactly once",
    "callback records returned execution ID before rerender",
    "periodic status polling remains separate",
    "stale recovery, watchdog, hard report timeout and quarantine remain verified",
    "valid report artifacts and final ZIP integrity remain verified"
  ],
  "live_required": [
    "hard-refresh browser once after deployment to clear stale fragment DOM",
    "confirm only one status panel is visible",
    "confirm click produces a new execution ID",
    "capture Render log and browser console if callback event still does not arrive"
  ]
}
