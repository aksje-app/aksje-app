{
  "version": "v18.6.77",
  "runtime_root_env": "APP_RUNTIME_ROOT",
  "default_runtime_root": ".app_runtime",
  "directories": {
    "data": "Mutable application state and local fallback data",
    "cache": "Rebuildable API and analysis cache",
    "logs": "Rotatable application and alert logs",
    "backups": "User-created runtime backup archives",
    "tmp": "Disposable temporary files"
  },
  "production_recommendation": "Use DATABASE_URL/Postgres for persistent state and APP_RUNTIME_ROOT for local cache/logs only."
}
